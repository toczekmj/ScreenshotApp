<Window x:Class="ScreenshotTestApp.Test.TestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:ScreenshotTestApp"
        xmlns:test="clr-namespace:ScreenshotTestApp.Test"
        mc:Ignorable="d"
        Title="TestWindow" Height="450" Width="800"
        >
    <Window.DataContext>
        <test:TestWindowViewModel/>
    </Window.DataContext>
    <Window.Background>
        <SolidColorBrush Color="{Binding BackgroundColor}" />
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        
        <Button Grid.Column="0" Grid.Row="2" Command="{Binding ChangeColorCommand}" CommandParameter="Beige" Margin="5" Click="Button_OnClick">Transparent Background</Button>
        <Button Grid.Column="1" Grid.Row="2" Command="{Binding ChangeColorCommand}" CommandParameter="Aqua" Margin="5" Click="Button_OnClick1">Color Background</Button>
        
        
    </Grid>
</Window>
